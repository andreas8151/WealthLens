import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  banner: {
    width: "85%",
    paddingVertical: 20,
    alignItems: "center",
    justifyContent: "center",
    marginBottom: 20,
    borderRadius: 10,
  },
  title: {
    fontSize: 20,
    fontWeight: "bold",
    color: "#fff",
  },
});
